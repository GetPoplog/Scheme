(a)
(car (cdr (list "fred" (+ 5 3) 6 7)))

8
----------------------------------------------------------------------------
( b)

 (append '(x y z) '(3 4 5))

(x y z 3 4 5)



( c)

(define (f x) (+ x y)) (let ((y 4)) (f 7))

Error: NUMBER(S) NEEDED
Culprits: Uninitialised variable 'y , 0,

(d)

(define (reduce f acc base l)
   (if (null? l)
       base
       (acc (f (car l)) (reduce f acc base (cdr l)))))


(reduce (lambda (x) (+ x 2)) + 0 '(4 5 6))

 ==> 21

 (e)

 (define fred (lambda x (car x)))

 (fred 4 5 6)

  ==> 4

----------------------------------------------------------------------------------
2
(assoc 'x '((y . 4) (x . 99) (z . 7) ))
==> (x . 99)






-----------------------------------------------------------------------------------------





 (usage
    '(define (f x)
        (lambda (y)
            (cons 'x
                (* x (+ y x)))
            ) )
    )
